{% extends 'base.twig' %}

{% block main %}
    <h1>Blog</h1>
    <ul>
        {% for file in dir('source/blog') %}
            {% if file != 'index.html' %}
            <li><a href="blog/{{ file }}">{{ file }}</a></li>
            {% endif %}
        {% endfor %}
    </ul>
{% endblock %}
